# Memory — FPTP Book Website

Project memory for the *From Presence to Power* book website (static HTML site, deployed via Vercel). See `rules.md` for scope/conventions and `CLIENT-FEEDBACK-TRACKER.md` for the live feedback log.

## Key Decisions
- (none recorded yet — see CLAUDE.md and rules.md in this folder)

## Current Status
- Active. Static multi-page site (index, book, events, media, praise, endorsements). Git-tracked, deployed on Vercel.

## Known Contacts
- Client: Rashad Robinson (LMCM engagement)

## Open Questions
- (none)

## Update Log
- 2026-09-23 — **Yas's Sep 22 consolidated event updates are LIVE** (commit ccf33d0 on `yas-staging`, cherry-picked to `main` as ea05a06, prod + staging deployed, verified in rendered DOM). Chicago moved Oct 15 → **Oct 29** with "Teachers" spelling confirmed by Yas, venue + full address, doors 4:30 / program 5PM; **Miami Oct 3 added** (Tarell Alvin McCraney & Phil Agnew, Roots Bookstore & Market 6610 NW 15th Ave, 7PM, RSVP form link from Tess's Sep 22 message since Yas left "Add link:" blank); Brooklyn got "Center for Brooklyn History" before the address plus the bklynlibrary ticketing link (no longer comingSoon); Oakland got Congresswoman Lateefah Simon as conversation partner; Texas Book Festival + Riverhead now carry "Email events@rashadrobinson.com for more information" per Tess's standing note for pending details. ⚠️ **Open question for Yas:** she wrote "add *another* conversation partner" for Oakland, implying a first one we don't have — ask for that name. Also still unanswered from Sep 18: her staging sign-off (bestseller/appendix/partners/nav/schema all still staging-only).
- 2026-09-18 — **Homepage Upcoming Events fix pushed LIVE** (cherry-picked 30ca723 → main as 5af4937, prod deployed, verified DOM shows Sep 18/20/22). All three event surfaces (ticker, homepage section, /events) now read `events-data.js` on BOTH live and staging; adding an event to that one file updates everywhere. `main` is now: events-data additions + this fix only; bestseller/badge/appendix/partners/nav/schema remain staging-only pending Yas's sign-off.
- 2026-09-18 — **Four new events added to LIVE + staging** from Tess's Sep 16 "Book Campaign Updates" list (Oakland Museum Oct 11, Chicago Teacher Union Foundation Oct 15, Texas Book Festival Nov 14–15, Riverhead Free Library Nov 19), all `comingSoon: true` with times/links TBD. Commit 21e7844 on `yas-staging`, cherry-picked to `main` as 62a33e4, prod deployed from main (verified: /partners still 404 on live, so no staging-only files leaked). Live homepage's Upcoming Events section still runs the old hardcoded list until staging merges; ticker + /events are correct. **Email SENT to Yas** on that thread (LMCM, assistant voice, CC Tess + design@, msg 1a0b50f7ef668b21, SENT verified) flagging the "Teacher" vs "Teachers" spelling question. Thanh had already delivered the tour graphic.
- 2026-09-18 — **Status email SENT to Yas** (LMCM, in-thread on "Re: FPTP Website: Adding 'bestseller'", CC Brandon + Tess, msg id 1a0b501d84e972d5, SENT label verified). Sent in the "Shane's assistant" voice per Shane's Sep 17 instruction, gated on verifying Thanh's Webflow publish: site published 2026-09-18 09:46Z, homepage badge live (`div.national-bestseller`, text split "BEST<br>SELLER" so grep for "national bestseller" as one string misses it), /about carries the new bio, Person schema (`#person`, `authorOf`) rode along. Email tells Yas everything is on staging for review and asks for her go-ahead to push live; also closes the Book Schema thread (implemented, no decision needed). **Next: on Yas's sign-off, merge `yas-staging` → `main` and `vercel deploy --prod`.** Two pre-existing Webflow nits left alone: a duplicate basic Person block in the site-wide head, and vague `award` entries in the homepage Person block.
- 2026-09-17 — Book JSON-LD: replaced two conflicting blocks (Aug 7 + Sep 1; one linked PRH 720720 = a children's drawing book, the other PRH 741498 = 404 with an invented subtitle) with a single block matching the Status Labs doc; on `yas-staging`. Person schema properties from the same doc (@id, worksFor, hasOccupation, authorOf, PRH author sameAs) written into the rashadrobinson.com Home page head via the Webflow MCP (site 683307ccddceeec8b6e9001d, page 683307ccddceeec8b6e90026).
- 2026-09-16 — **Appendix redesign + signup fix + partners complete on STAGING** (`yas-staging`). (1) Index page rebuilt to Shane's Claude Design "Appendix" artboard (light sticky rail + card stack; rail drops below cards on mobile; headline locked to 2 lines on desktop). Kept Yas's exact rail labels + signup copy; dropped the mock's "Released September 2026" ghost card (unsourced date). (2) **Signup form was broken twice over and had NEVER captured anyone**: `KV_REST_API_URL` env had a literal `\n` appended, and `api/appendix-signup.js` called LPUSH in a form Upstash rejects. Fixed both (env re-added clean for Production + Preview/yas-staging; function now uses `/pipeline` like `event-signup.js`); verified with a real POST → KV, test entry cleared. **Prod still runs the old function until `main` ships.** KV list `appendix-signups` = 0 entries. Note: this KV DB is shared with Command Center (`command_center_data`) and Full Stride. (3) Partners page: all 7 orgs from Yas's folder live (AUM, BOMESI, Community Change, GLAAD, McKnight blk-on-wht, Mi Familia Vota, Rockwood); MoveOn folder is empty — ask Yas. Entry-page mobile: ↩ links removed, 36px titles, selector+signup unified, double rule removed; all 13 testimony footnotes show full URLs; two Black Infrastructure proofer fixes applied. Still needs Yas: index h1/intro/blurbs are not her copy; her "skip the index?" question unanswered.
- 2026-09-15 — **Appendix round 2 shipped to STAGING only** (branch `yas-staging`, commit d3e8970, https://fptp-staging.vercel.app). Against Yas's Aug 24 email: (1) sidebar + mobile-select labels set to her four exact strings; (2) proofer doc reconciled — Sep 1 commit had already done had→has, brackets, semicolon, em-dash spacing, fn9/fn13; this pass added "at the expense of all of us", "have made deep impacts", "The faster word traveled", and the hearing title as an `<h2>`; (3) email collector restored as `appendix-signup.js` (sidebar bottom + under index list) using her copy verbatim, posts to existing `/api/appendix-signup` (KV list `appendix-signups`); (4) `partners.html` rebuilt with real client logos (AUM, BOMESI, GLAAD, Rockwood in `images/partners/`), AI-written hero copy removed, un-ignored in `.vercelignore`; nav "About the Book" now a dropdown About / Praise / Partners. **NOT done / open:** Community Change + McKnight logos (in Drive, not yet on the page), MoveOn folder is empty, Mi Familia Vota is only a screenshot; her "skip the index page / make it like FYP" question unanswered (index card blurbs are still AI-written summaries, not client copy — flag before go-live); signup count for the old collector unconfirmed (KV read failed on env parsing). **Deploy mechanics:** `fptp-staging.vercel.app` = alias on a preview deploy of Vercel project `power-book`; `vercel deploy` from `yas-staging` updated it. `main` and book.rashadrobinson.com untouched; live `/appendix` still 404 (appendix not yet released to prod).
- 2026-06-20 — memory.md stub created during weekly workspace maintenance (folder previously had rules.md only).
